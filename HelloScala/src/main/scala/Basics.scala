object Basics extends App{
  // Define a Value
  val meaningOfLife: Int = 42
  val newMeaningOfLife = 46

  def myFunctions(x: Int, y: Int) : Unit = {
    print(x + " " + y)
  }

  myFunctions(1,5)
}
